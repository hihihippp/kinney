<%= link_to(person, :class => [:thumbnail, :person_read_more, :clearfix]) do %>
  <%= kinney_image_small(person.image, person) if person.image %>
  <div class="bio">
    <%= person.short_bio %>
  </div>
  <%= t 'kinney.clips.show.read_more_about', :name => person.full_name %>     
<% end -%>