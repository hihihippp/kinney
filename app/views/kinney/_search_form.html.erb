<%= form_tag search_path, :method => :get, :class => ['navbar-search'] do %>  
  <div class="input-append">
    <%#= label_tag :q, t('kinney.search.label') %>
    <%= text_field_tag :q, params[:q], :class => 'span2 search-query', :placeholder => t('kinney.search.placeholder') %><%= submit_tag t('kinney.search.submit_text'), :name => nil, :class => ['add-on'] %>
  </div>
<% end %>
